development:
  clients:
    default:
      database: linguita_development
      hosts:
        - localhost:27017
production:
	sessions:
	  default:
	    uri: <%= ENV['MONGODB_URI'] %>
	    options:
	      safe: true